<div classted_at="container">
  <div class="row">
    <div class="twelvecol">
      <div class="content">
        <h2>Actions for incidences</h2>
        <p>
          <ul>
           <li>#{IncidencesController.a('New',:new)}</li>
          </ul>
        </p>     
        <div class="listing">
          <table>
            <thead>
              <tr>
                <th>ID</th>
                <th>Centro</th>
                <th>Descripcion</th>
                <th>Fecha alta</th>
                <th>Estado</th>
                <th>Enviado</th>
                <th>Action</th>
                <th>Correo</th>
              </tr>
            </thead>
            <tbody>
              <?r @incidences.each do |key,value| ?>
              <tr>
                <td>#{IncidencesController.a(key[:name],:details,key[:id])} </td>
                <td>#{key[:branch]}</td>
                <td>#{key[:problem]}</td>
                <td>#{key[:created_at]}</td>
                <td>
                  <?r if #{key.motive_id}.to_i == 1 ?>
                  <abbr title="Acuse de Recibos">
                   <span style="font-weight: bold; color: red;">Acuse de Recibo #{key.motive_id}</span>
                  </abbr>
                  <?r else ?>
                  <abbr title="Pendiente">
                    <span style="font-weight: bold; color: orange;">Pendiente</span>
                  </abbr>
                  <?r end ?>
                </td>
                <td>
                <?r if key[:motive_id].to_i==1 ?>
                 #{key[:sended_ack]}
                <?r end ?>
                <?r if key[:motive_id].to_i==2 ?>
                 #{key[:sended_pending]}
                <?r end ?>
                <?r if key[:motive_id].to_i==3 ?>
                 #{key[:sended_solution]}
                <?r end ?>
                </td>
                <td>#{IncidencesController.a('Editar',:edit,key[:id])}</td>
                <td>
                <?r if key[:motive_id].to_i==1 ?>
                  #{IncidencesController.a('Enviar',:sending_ack,key[:id])} </td>
                <?r end ?>
                <?r if key[:motive_id].to_i==2 ?>
                  #{IncidencesController.a('Enviar',:sending_pending,key[:id])} </td>
                <?r end ?>
                <?r if key[:motive_id].to_i==3 ?>
                  #{IncidencesController.a('Enviar',:sending_solution,key[:id])} </td>
                <?r end ?>  
                </td>
              </tr>
             <?r end ?>
            </tdbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div> 
